# quantum = RustQIP statevector (default, desktop + browser)
# classic = uniform random
# qiskit = Qiskit Aer via Python (desktop only)
QUANTUM_MODE=quantum

# Optional: point Qiskit mode/tests at a virtualenv.
# QUANTUM_PYTHON=.venv/bin/python
