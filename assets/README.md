# Assets

Optionnel — le jeu fonctionne sans (sous-marin procédural + sol Bevy).

| Fichier | Rôle |
| --- | --- |
| `models/rock_largeA.glb` | Gros récifs |
| `models/rock_smallA.glb` | Petits récifs |

Téléchargement (Kenney Nature Kit, CC0) :

```bash
./scripts/fetch_assets.sh
```
